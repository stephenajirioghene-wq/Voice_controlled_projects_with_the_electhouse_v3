Readme..
This code, handles the training of two commands on the vr module simultaneously.
in the output function, you can add any thing after the serialprints.
replace the serial prints with lcd prints to use lcd.


the  button isr is connected to pin 2 with arduino pull up
the vr is connected to pins 3 and 5